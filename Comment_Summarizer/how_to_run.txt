To run the project, follow these steps:1. In the project directory, execute the following command to install the required dependencies: “pip install -r requirements.txt”2. To launch the Flask application for user interaction, run the following command: “python app.py”3. For executing the machine learning models, open the "ML_Group_4.ipynb" notebook in Google Colab with GPU access(A-100) and run all cells.